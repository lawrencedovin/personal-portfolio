import React from 'react';
import PropTypes from 'prop-types';


const Hero = () => (
  <div data-testid="Hero">
    Hero Component
  </div>
);

Hero.propTypes = {};

Hero.defaultProps = {};

export default Hero;
