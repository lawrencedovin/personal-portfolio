import React from 'react';
import PropTypes from 'prop-types';


const Hero = () => (
  <div className="hero" data-testid="Hero">
    
  </div>
);

Hero.propTypes = {};

Hero.defaultProps = {};

export default Hero;
